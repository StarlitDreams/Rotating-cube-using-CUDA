# Ignore everything by default
*

# Keep directories so we can unignore files inside them
!*/

# Track ONLY CUDA source files anywhere
!*.cu
!*.cuh

# Always keep this file, so the rules survive
!.gitignore

# (Optional) keep README & license
!README.md
!LICENSE*
